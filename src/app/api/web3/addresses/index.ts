// Re-export address utilities
export { isValidAddress, getAddresses } from '../utils/addresses'

// Re-export contract addresses
export {
  TIER_CONTRACT_ADDRESS,
  PRO_CONTRACT_ADDRESS,
  GROUP_CONTRACT_ADDRESS,
  CONTRACT_ADDRESSES,
} from './contracts'
